// 판매 상태 정보 입력 컴포넌트 => 두개의 버튼으로 설정하거나 안하거나로 구분하면 될듯

import { Box, Description, RequireDot, Title } from './RegisterTitle';

const RegisterStatus = () => {
  return <Box>RegisterStatus</Box>;
};

export default RegisterStatus;
