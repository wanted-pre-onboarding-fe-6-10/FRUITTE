import { Box, Description, RequireDot, Title } from './RegisterTitle';

// textarea로 Description 부분을 입력하는 컴포넌트 구성
const RegisterDescription = () => {
  return <Box>RegisterDescription</Box>;
};

export default RegisterDescription;
