// 옵션 입력 컴포넌트, 옵션명, 가격, 재고수량 입력할 수 있는 컴포넌트

import { Box, Description, RequireDot, Title } from './RegisterTitle';

const RegisterOption = () => {
  return <Box>RegisterOption</Box>;
};

export default RegisterOption;
