import styled from 'styled-components';

const GoodsReview = () => {
  return (
    <Box>
      <Wrapper>GoodsReview</Wrapper>
    </Box>
  );
};

const Box = styled.div``;
const Wrapper = styled.div``;

export default GoodsReview;
