import styled from 'styled-components';

const GoodsInfo = () => {
  return (
    <Box>
      <Wrapper>GoodsInfo</Wrapper>
    </Box>
  );
};

const Box = styled.div``;
const Wrapper = styled.div``;

export default GoodsInfo;
